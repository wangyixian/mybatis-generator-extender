//plugins {
//    id 'java'
//    id 'idea'
//}
apply plugin: 'java'
apply plugin: 'idea'
//group 'com.edo.mybatis.generator'
//version '1.0-SNAPSHOT'

jar {
    baseName = 'mybatis-generator-extender'
    version = '0.1.0'
//    manifest.attributes 'Main-Class': 'com.edo.mybatis.Application'
//    into('lib') {
//        from configurations.runtime
//    }
    from {
        //添加依懒到打包文件
        //configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
        configurations.runtime.collect{zipTree(it)}
    }
    manifest {
        attributes 'Main-Class': 'com.edo.mybatis.Application'
    }
}

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'

    compile files('libs/mybatis-generator-core-1.3.2.jar')
    compile files('libs/mysql-connector-java-5.1.13-bin.jar')
}
